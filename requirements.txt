# Core
pip>=25.0
setuptools>=75.0
wheel

# NumPy – must be latest dev-compatible
numpy>=2.1.0

# Pandas – try newest only
pandas>=2.2.0

# Cython – REQUIRED
cython>=3.0.10

# Avoid scipy stack if possible
